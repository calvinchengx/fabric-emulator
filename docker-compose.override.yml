# Auto-loaded alongside docker-compose.yml by `docker compose up` (Compose
# merges *.override.yml automatically when present — no -f flag needed): adds
# REAL engines to the default experience — LakeSail's Sail (Rust Spark-Connect,
# no JVM) behind the Livy statement agent — native Livy sessions + notebook
# cell execution on a real engine, and a SQL Server sidecar for
# the T-SQL/TDS warehouse surface (Warehouse, Lakehouse SQL endpoint, Fabric SQL
# Database) — so `docker compose up` gives you the emulator with real compute
# attached, not just the contract. See docs/14-real-compute.md.
#
# Opt out — the lite, contract-only pair (no heavy sidecars, honest 501s on the
# Spark/SQL surfaces), by naming the base file explicitly so Compose skips this
# override:
#   docker compose -f docker-compose.yml up
#
# Local dev-only credentials below (not for anything internet-facing).
services:
  # One long-lived SparkSession, a REPL over HTTP — the emulator terminates the
  # Livy REST protocol itself and drives this agent (no Apache Livy server; it's
  # retired). See e2e/livy/agent.py.
  # LakeSail's Sail: a Rust Spark-Connect server — the engine behind the agent.
  # No JVM anywhere; PySpark clients can also connect directly at
  # sc://localhost:50051. See docs/20-lakesail-engine.md.
  sail:
    # image + build: `docker compose pull` fetches the published sidecar (so a
    # pinned SAIL_VERSION is reproducible), while `docker compose build` still
    # works from source for local development. Defaults to latest, like the rest.
    image: ghcr.io/calvinchengx/fabric-emulator-sail:${SAIL_VERSION:-latest}
    build:
      context: .
      dockerfile: docker/sail/Dockerfile
    depends_on:
      fabric-emulator:
        condition: service_healthy
    ports:
      - "50051:50051" # Spark Connect (plain h2c gRPC; no auth — local use only)
    environment:
      # The launcher mints AZURE_STORAGE_TOKEN from entra before exec'ing sail
      # (object_store reads credentials from process env at startup only).
      ENTRA_TOKEN_URL: "https://entra-emulator:8443/11111111-1111-1111-1111-111111111111/oauth2/v2.0/token"
      ENTRA_CLIENT_ID: "cccccccc-0000-0000-0000-000000000002"
      ENTRA_CLIENT_SECRET: "daemon-app-secret"
      # az://{workspace}/{item}/… → the emulator's account-prefixed Blob path.
      AZURE_STORAGE_ACCOUNT_NAME: "onelake"
      AZURE_STORAGE_ENDPOINT: "https://fabric-emulator:9443/onelake"
      AZURE_ALLOW_INVALID_CERTIFICATES: "true" # emulator's self-signed TLS
      SAIL_SPARK__SESSION_TIMEOUT_SECS: "3600"
      RUST_LOG: "info"
    # Without this the container reports `running` forever whether or not the
    # engine ever bound its port, so nothing downstream can wait on it. Plain
    # TCP because the surface is h2c gRPC and the image carries no gRPC client;
    # proving the port listens is the honest scope of a healthcheck. The deeper
    # "does Spark compute" question is `scripts/status.sh --spark`.
    healthcheck:
      test: ["CMD", "python", "-c",
             "import socket; socket.create_connection(('127.0.0.1', 50051), 2).close()"]
      interval: 5s
      timeout: 3s
      retries: 20

  spark-agent:
    image: ghcr.io/calvinchengx/fabric-emulator-spark-agent:${SPARK_AGENT_VERSION:-latest}
    build:
      context: .
      dockerfile: docker/python-runtime/Dockerfile
      # sail-delta, not spark-connect: the agent routes OPTIMIZE/VACUUM/CDF
      # through delta-rs (e2e/livy/delta_ops.py), so deltalake belongs in the
      # image. With spark-connect the import failed and the interception
      # silently never installed.
      args: { UV_GROUP: sail-delta }
    depends_on:
      sail:
        # Now that sail reports health, wait for the engine to actually listen
        # rather than merely be started: the agent holds a Spark Connect client.
        condition: service_healthy
    volumes:
      # The directory, not just agent.py: the agent imports delta_ops and
      # storage from beside itself.
      - ./e2e/livy:/livy:ro
    environment:
      SPARK_REMOTE: "sc://sail:50051" # Spark Connect client — no JVM
      # The delta-rs path talks to OneLake directly, so it needs its own
      # credentials — a Connect client cannot read back the bearer sail holds.
      # Same issuer, same Storage audience, so it acts as the same principal.
      ENTRA_TOKEN_URL: "https://entra-emulator:8443/11111111-1111-1111-1111-111111111111/oauth2/v2.0/token"
      # Notebook attribution for the delta-rs path: Rust object_store cannot set
      # request headers, so the agent mints a bearer carrying fabric_job_id /
      # fabric_cell_index as extra claims and the emulator reads them off the
      # validated principal. See internal/onelake/observe.go.
      ENTRA_FORGE_URL: "https://entra-emulator:8443/admin/api/tokens"
      ENTRA_CLIENT_ID: "cccccccc-0000-0000-0000-000000000002"
      ENTRA_CLIENT_SECRET: "daemon-app-secret"
      AZURE_STORAGE_ACCOUNT_NAME: "onelake"
      AZURE_STORAGE_ENDPOINT: "https://fabric-emulator:9443/onelake"
      AZURE_ALLOW_INVALID_CERTIFICATES: "true" # emulator's self-signed TLS
    command: python /livy/agent.py
    # The agent already serves /health (e2e/livy/agent.py); it just was not wired
    # up, so `running` was never evidence the statement executor could answer.
    healthcheck:
      test: ["CMD", "python", "-c",
             "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8099/health', timeout=3)"]
      interval: 5s
      timeout: 5s
      retries: 20

  sqlserver:
    # Digest-pinned for the same reason as kustainer: this sidecar IS the warehouse
    # engine, so an unannounced change moves T-SQL behaviour under the tests. Refresh:
    #   docker buildx imagetools inspect mcr.microsoft.com/mssql/server:2022-latest
    image: mcr.microsoft.com/mssql/server:2022-latest@sha256:ba4c8329f48fb8f02e1416be6a930ebfd71268caee78aa985f3af4315e457c89
    # amd64 only — Microsoft publishes no arm64 manifest for mssql/server. On an
    # Apple-silicon host the engine translates this one container (Rosetta);
    # stating the platform beats relying on per-engine fallback, which can also
    # fail outright with "no matching manifest for linux/arm64/v8". Everything
    # else in this stack has a native arm64 build, so nothing else pays for it.
    platform: linux/amd64
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "Str0ng!Passw0rd"
    healthcheck:
      test:
        - CMD
        - /opt/mssql-tools18/bin/sqlcmd
        - -S
        - localhost
        - -U
        - sa
        - -P
        - "Str0ng!Passw0rd"
        - -C
        - -Q
        - "SELECT 1"
      interval: 5s
      timeout: 5s
      retries: 40
      start_period: 20s

  fabric-emulator:
    environment:
      # Native Livy: terminate the protocol and drive the agent above — real
      # interactive sessions, notebook cell execution, high-concurrency REPLs.
      FABRIC_SPARK_AGENT_URL: "http://spark-agent:8099"
      # The T-SQL/TDS warehouse surface, relaying to the SQL Server sidecar
      # above. Generous dial/connection timeouts: the backend dials lazily on
      # first query, and the sidecar (esp. an amd64 image emulated on arm64)
      # can take a while to finish booting.
      FABRIC_SQL_TDS_ADDR: ":1433"
      FABRIC_WAREHOUSE_SQL_URL: "server=sqlserver;user id=sa;password=Str0ng!Passw0rd;encrypt=disable;dial timeout=90;connection timeout=90"
    # Restate the base port mapping alongside the new one — Compose replaces
    # (not merges) list-typed fields like `ports` across files, so the full
    # desired list must be given here.
    ports:
      - "9443:9443" # api.fabric + onelake.dfs + portal (host-routed)
      - "1433:1433" # warehouse T-SQL/TDS (FedAuth; sqlcmd/pyodbc/dbt-fabric point here)
