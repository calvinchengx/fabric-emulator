| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |# 12 — E2E matrix
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |What CI proves on every push, and what's queued. The bar, inherited from
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |entra-emulator's SDK matrix: **real clients, unmodified**, against the
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |emulator — because driving Microsoft's actual tools catches fidelity gaps
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |spec-reading cannot (see [what fabric-cicd caught](11-testing-with-fabric-cicd.md#what-driving-the-real-tool-caught)).
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |## Verified on every push
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) || Suite | Client | Proves | Where |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) ||---|---|---|---|
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) || Token handshake | in-process **entra-emulator** | real client-credentials token (Fabric aud) → JWKS validation → full workspace/RBAC/item/LRO flow over HTTP | Go integration tests (CI `test` job, Linux + macOS + Windows) |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) || Git round-trip | Go HTTP | two-workspace commit→update, definitions intact, logical ids preserved | Go integration tests |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) || Identity handshake | in-process entra-emulator | provision → entra mints for the identity → token passes fabric RBAC → deprovision revokes → delete cascades | Go integration tests |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) || OneLake | Go HTTP + real entra Storage tokens | create/append/flush/read via GUID + name addressing, listings, RBAC walls, managed-folder rejections | Go integration tests |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) || **fabric-cicd** | Microsoft's real Python tool (v1.2.x) | `publish_all_items` publishes a notebook; parts round-trip byte-for-byte | `e2e/fabric-cicd/run.py` (CI `fabric-cicd` job) |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |Plus: coverage floor 90% (cross-package; currently ~93%), `go vet`, and the
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |[docs site](https://calvinchengx.github.io/fabric-emulator/) build on every
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |docs push.
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |## Queued (designed, not yet wired)
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) || Suite | Client | Proves | Blocked on |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) ||---|---|---|---|
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) || Delta write/read (**A1**) | `deltalake` (delta-rs) | a real engine writes a Delta table through our DFS with an entra Storage token | R0 storage completeness ([14-real-compute.md](14-real-compute.md)) |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) || Spark via ABFS (**A2**) | real PySpark + delta-spark | `abfss://…@onelake.dfs…` + OAuth against entra; cross-engine read-back with delta-rs | R1 |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) || azcopy / ADLS SDK | Microsoft storage tooling | the DFS wire subset satisfies stock ADLS clients | subsumed by A1/A2 |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |## Running locally
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |```bash
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |go test ./...              # everything in-process, no network
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |python3 e2e/fabric-cicd/run.py   # the real-tool e2e (needs Python 3 + go)
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |```
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |
| Delta write/read (**A1**) ✅ | `deltalake` (delta-rs) | real engine writes/reads a Delta table through the OneLake Blob surface with an entra Storage token; `e2e/delta-rs`, CI | shipped (R0) |Both are deterministic: virtual clock, in-memory stores, seeded credentials.
