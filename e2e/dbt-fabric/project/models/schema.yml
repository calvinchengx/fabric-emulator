version: 2

models:
  - name: stg_countries
    description: "Countries staged from the seed — exercises view build + tests over TDS."
    columns:
      - name: id
        tests:
          - unique
          - not_null
      - name: name
        tests:
          - not_null
